<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Simulated OBi200: REGISTER (digest auth) then place a call to [service],
  play a PCMU pcap of synthesized speech, hold the call so Hermes can reply,
  then hang up.

  Keywords supplied by entrypoint.sh:
    [user]      SIP username / AoR  (-key user)
    [service]   dialed extension    (-s)
    [pause_ms]  hold time after audio (-key pause_ms)
    [authentication] fills digest from -au/-ap on a 401 challenge.

  pjsip challenges each transaction, so both REGISTER and INVITE do the
  no-auth → 401 → auth retry dance. If your Asterisk answers 407 instead of
  401, change the two `response="401"` lines to `response="407"`.
-->
<scenario name="OBi200 register + call + play + hangup">

  <!-- ── REGISTER ─────────────────────────────────────────────────────── -->
  <send retrans="500">
    <![CDATA[
      REGISTER sip:[remote_ip] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[user]@[remote_ip]>;tag=[call_number]
      To: <sip:[user]@[remote_ip]>
      Call-ID: [call_id]
      CSeq: [cseq] REGISTER
      Contact: <sip:[user]@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Expires: 300
      Content-Length: 0

    ]]>
  </send>
  <recv response="401" auth="true"/>

  <send retrans="500">
    <![CDATA[
      REGISTER sip:[remote_ip] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[user]@[remote_ip]>;tag=[call_number]
      To: <sip:[user]@[remote_ip]>
      Call-ID: [call_id]
      CSeq: [cseq] REGISTER
      Contact: <sip:[user]@[local_ip]:[local_port];transport=[transport]>
      [authentication]
      Max-Forwards: 70
      Expires: 300
      Content-Length: 0

    ]]>
  </send>
  <recv response="200"/>

  <!-- ── INVITE (dial the Stasis extension) ───────────────────────────── -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[user]@[remote_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]>
      Call-ID: [call_id]
      CSeq: [cseq] INVITE
      Contact: <sip:[user]@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=obi200 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=hermes-e2e
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-15
      a=sendrecv
    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="401" auth="true"/>

  <!-- ACK the challenge, then re-INVITE with credentials. -->
  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[user]@[remote_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] ACK
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[user]@[remote_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]>
      Call-ID: [call_id]
      CSeq: [cseq] INVITE
      Contact: <sip:[user]@[local_ip]:[local_port];transport=[transport]>
      [authentication]
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=obi200 53655765 2353687638 IN IP[local_ip_type] [local_ip]
      s=hermes-e2e
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-15
      a=sendrecv
    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>
  <recv response="200" rtd="true" rrs="true"/>

  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[user]@[remote_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] ACK
      Contact: <sip:[user]@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- ── Play the spoken phrase as PCMU into the call ─────────────────── -->
  <nop>
    <action>
      <exec play_pcap_audio="/tmp/say.pcap"/>
    </action>
  </nop>

  <!-- Hold the call up so Hermes can transcribe, think, and speak back. -->
  <pause milliseconds="[pause_ms]"/>

  <!-- ── Hang up ──────────────────────────────────────────────────────── -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[user]@[remote_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] BYE
      [authentication]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>
  <recv response="200" crlf="true"/>

  <ResponseTimeRepartition value="100, 200, 400, 800, 1600, 3200"/>
  <CallLengthRepartition value="10, 20, 30, 40, 50, 100, 200"/>
</scenario>
